variables:
  IMAGE_REGISTRY: docker.io
  IMAGE_NAME: teasaita/nonotes_app
  TAG_LATEST: $IMAGE_REGISTRY/$IMAGE_NAME:latest
  TAG_COMMIT: $IMAGE_REGISTRY/$IMAGE_NAME:$CI_COMMIT_SHORT_SHA
  DOCKER_TLS_CERTDIR: ""

default:
  image: docker:latest
  services:
    - docker:latest
  before_script:
    - docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD $IMAGE_REGISTRY

stages:
  - build
  - test

build:commit:
  stage: build
  script:
    - docker pull $TAG_LATEST || true
    - docker build --force-rm --cache-from $TAG_LATEST -t $TAG_COMMIT .
    - docker push $TAG_COMMIT
  after_script:
    - docker rmi $IMAGE_NAME:eeae4481
    - docker rmi $IMAGE_NAME:cf49823c
    - docker rmi $IMAGE_NAME:212aff67
    - docker rmi $IMAGE_NAME:9714e804
    - docker rmi $IMAGE_NAME:fef48c81
    - docker rmi nonotes-app:latest
    - docker rmi $IMAGE_NAME:$CI_COMMIT_SHORT_SHA
    - docker images

build:latest:
  stage: build
  script:
    - docker pull $TAG_LATEST || true
    - docker build --force-rm --cache-from $TAG_LATEST -t $TAG_LATEST .
    - docker push $TAG_LATEST
  rules:
    - if: $CI_COMMIT_BRANCH == 'master'
  after_script:
    - docker images
    - docker rmi $IMAGE_NAME:latest
    - docker images

# rubocop:bundle_audit:
#   stage: test
#   image: 
#   script: |
#     gem install bundler-audit
#     bundle-audit update
#     bundle-audit check

tests:units:
  stage: test
  script:
    - docker pull $TAG_COMMIT
    - docker images
    - docker-compose up -d
    - sleep 20
    - docker ps
    - docker-compose exec -T app bundle exec rspec spec
  after_script:
    - docker-compose down -v --remove-orphans || true